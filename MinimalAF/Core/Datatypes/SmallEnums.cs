namespace MinimalAF {
    public enum VAlign {
        Bottom, Center, Top
    }

    public enum HAlign {
        Left, Center, Right
    }

    public enum MouseButton {
        Left,
        Right,
        Middle,
        Any
    }

    /// <summary>
    /// Defines how the ending cap of a line should look.
    /// </summary>
    public enum CapType {
        None,
        Circle
    }
}
